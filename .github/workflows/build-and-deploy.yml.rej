diff a/.github/workflows/build-and-deploy.yml b/.github/workflows/build-and-deploy.yml	(rejected hunks)
@@ -63,7 +63,7 @@ jobs:
         run: sudo apt install rsync
 
       - name: Generate static directory listing
-        run: $(go env GOPATH)/bin/statik -l -b "https://${{ github.event.repository.owner.name }}.github.io/${{ github.event.repository.name }}/" -i "\.(pdf|link|txt)$" -e "\.git(hub|ignore|keep)?"
+        run: $(go env GOPATH)/bin/statik -header .github/workflows/header.gohtml -l -b "https://${{ github.event.repository.owner.name }}.github.io/${{ github.event.repository.name }}/" -i "\.(pdf|link|txt)$" -e "\.git(hub|ignore|keep)?"
 
       - name: Generate site artifact
         if: github.ref == 'refs/heads/main'
